package main.java.pojo.community.group;

public class CloseAndSaveGroupFields {
    private boolean is_active;

    public boolean isIs_active() {
        return is_active;
    }

    public void setIs_active(boolean is_active) {
        this.is_active = is_active;
    }
}
